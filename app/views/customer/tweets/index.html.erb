<% @tweets.each do |tweet| %>
<div>
  <%= attachment_image_tag tweet, :tweet_image %>
  <p>投稿ユーザー画像：</p>
  <p>タイトル：<%= tweet.title %></p>
  <p>内容：<%= tweet.body %></p>
  <p>ユーザーネーム：<%= tweet.customer.name %></p>
</div>
<% end %>